# Netlify config. The site lives in web/; everything else (Go template, CI) is
# ignored by the build context.
[build]
  base = "web"
  command = "npm ci && npm run build:deploy"
  publish = "dist"

[build.environment]
  # Vite 6 needs Node 18+.
  NODE_VERSION = "20"
